/**
 * Point d'entrée du paquet `lea-tuteur-ia`.
 *
 * Phase 0 (§12) : ré-export du baril des contrats uniquement. Aucune logique
 * métier — « jamais la largeur d'abord » (§12).
 */

export type * from './contracts/index.js';
